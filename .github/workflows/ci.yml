name: Upload reports to codecov
on: [push]
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Python 3.8
        uses: actions/setup-python@v2
        with:
          python-version: 3.8
      - name: Install coverage
        run: pip install coverage
      - name: Run unittests and collect coverage
        run: |
          coverage run test_Graph_API.py
          coverage xml reports/unittest.xml
      - name: Upload unit tests coverage to Codecov
        uses: codecov/codecov-action@v2
        with:
          files: ./reports/unittest.xml
          fail_ci_if_error: true
          flags: unittest
          
      - name: Run e2e tests and collect coverage
        run: |
          coverage run test_Menu.py
          coverage xml reports/e2etest.xml
          
      - name: Upload e2e tests coverage to Codecov
        uses: codecov/codecov-action@v2
        with:
          files: ./reports/e2etest.xml
          fail_ci_if_error: true
          flags: e2etest
        
